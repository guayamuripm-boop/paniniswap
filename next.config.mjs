/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // reactCompiler: true, // Desactivado: causaba error "Cannot read properties of null (reading 'id')"
};

export default nextConfig;
